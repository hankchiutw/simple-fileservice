'use strict';
/** @module */

const chai = require('chai');
const assert = chai.assert;
chai.use(require('chai-as-promised'));

/**
 * Customized asynchronous assert module 
 */
module.exports = assert;
